export function HangTimeApp() {
  return (
    <div className="mx-auto flex min-h-dvh max-w-5xl flex-col gap-6 px-4 py-10">
      <h1 className="font-display text-5xl font-extrabold tracking-wide text-fg">HangTime</h1>
      <p className="text-sm text-muted">
        Full jumptime-app.tsx from Drive is staged locally at
        /workspace/hangtime/slim/src/components/jump/jumptime-app.tsx (23969 chars).
        Re-push that file via GitHub MCP push_files or a local git push once SCM is connected.
      </p>
    </div>
  );
}
